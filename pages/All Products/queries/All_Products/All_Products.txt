SELECT 
    ap.product_id, 
    ap.parent_id, 
    ap.product_type, 
    ap.sku, 
    ap.name, 
    pm_price.meta_value AS price,
    ap.main_image_url, 
    ap.product_url, 
    ap.all_categories, 
    ap.hasVariants, 
    s.name AS supplier,
		s.discount AS supplier_discount,
		ps.markup_percent AS product_markup,
		ps.aq_list_price,
		ps.id as product_supplier_id
FROM all_products ap
LEFT JOIN product_suppliers ps 
    ON ap.product_id = ps.product_id
LEFT JOIN suppliers s 
    ON ps.supplier_id = s.id
LEFT JOIN WrARJWM7_postmeta pm_price 
    ON ap.product_id = pm_price.post_id 
    AND pm_price.meta_key = '_price'
WHERE ap.parent_id = 0;
