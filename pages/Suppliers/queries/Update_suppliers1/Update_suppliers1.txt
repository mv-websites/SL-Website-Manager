UPDATE wp_qyxos.WrARJWM7_suppliers
SET 
    name = '{{ Table1.updatedRow.name }}',
    discount = {{ Table1.updatedRow.discount || 0 }},
    discount_confirmed = {{ Table1.updatedRow.discount_confirmed ? 1 : 0 }},
		notes = '{{ (Table1.updatedRow || {}).notes || "" }}'
WHERE id = {{ Table1.updatedRow.id }};
