
INSERT INTO WrARJWM7_suppliers (name, date_adjusted, discount, discount_confirmed, notes) 
VALUES (
  '{{ (Table1.newRow || {}).name }}',
  '{{ moment().format("YYYY-MM-DD") }}',
  '{{(Table1.newRow || {}).discount || 0}}',
  {{ (Table1.newRow || {}).discount_confirmed ? 1 : 0 }},
  '{{ (Table1.newRow || {}).notes || "" }}'
);
