
{{(() => {
  // Look for reactive_jobs in meta_data
  const existing = Table1.updatedRow.clientRefObject;

  return {
    meta_data: existing ? [
          {
            id: existing.id, 
            key: "client_ref",
            value: Table1.updatedRow.ClientRef
          }
        ]
      : [
          {
            key: "client_ref",
            value: Table1.updatedRow.ClientRef
          }
        ]
  };
})()}}

